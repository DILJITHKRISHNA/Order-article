import { AdminPage } from "@/components/admin/admin-page";

export const metadata = {
  title: "Admin | Order Management",
  description: "Admin view for all submitted orders",
};

export default function AdminRoutePage() {
  return <AdminPage />;
}
